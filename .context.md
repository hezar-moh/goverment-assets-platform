# GovAsset Platform — Anchored Summary

## Documentation (5 Consolidated Files)

| File | Covers |
|------|--------|
| `01_PROJECT_CONCEPTS_AND_ARCHITECTURE.md` | System overview, architecture, multi-tenancy, roles, IP/domains, beginner foundation, request lifecycle, code map, settings.py deep dive, misconceptions, JWT structure, login flow diagrams, government scenarios |
| `02_KEYCLOAK_AND_AUTHENTICATION.md` | Keycloak admin walkthrough, auth vs authorization, two auth paths, Django↔Keycloak sync, custom attributes, SSO login flow, JWT login flow, JWT tokens, security features, external system integration (direct API + SSO redirect), API endpoint reference, test accounts |
| `03_DEPLOYMENT_LOCAL_AND_RAILWAY.md` | Local dev startup, Railway components, local vs Railway comparison, environment variables reference, database access (pgAdmin, psql), two ports explained, deployment workflow, troubleshooting |
| `04_TESTING_AND_RESPONSIVE_UI.md` | Unit tests (pytest, 83 tests), integration tests (Postman, 13 tests), load tests (Locust), security tests (OWASP ZAP), manual testing checklist, responsive UI overview, CSS attribute inventory, panel Q&A |
| `05_PRESENTATION_DEFENSE_COMPLETE_GUIDE.md` | Pre-presentation checklist, opening script, local demo walkthrough (13 steps), Railway demo walkthrough (4 steps), features to show, elevator pitches (1/3/5 min), panel Q&A (80+ questions, 8 groups), CSS quick reference, architecture diagrams, closing statements, defense strategies, confidence tips |

## Key Architectural Decisions

1. **Multi-tenancy via django-tenants** — Each ministry gets its own PostgreSQL schema (`moh_schema`, `mof_schema`, etc.). Schema isolation at database level.
2. **JWT + session dual auth** — Mobile app uses JWT tokens (API). Web browser uses Keycloak SSO + Django sessions.
3. **Keycloak for SSO** — Keycloak handles passwords. Django never stores passwords. Flows: web SSO, API login via custom backend, API token-based (mobile).
4. **Role-Based Access Control** — 5 roles: SUPER_ADMIN, MINISTRY_ADMIN, AGENCY_MANAGER, FACILITY_CLERK, AUDITOR.
5. **Single database, multiple schemas** — django-tenants routes requests to the correct schema based on domain or user profile.

## Quick Reference — Files to Edit for Common Changes

| Change | File(s) |
|--------|---------|
| Add a new app | `config/settings.py` (add to SHARED_APPS or TENANT_APPS) |
| Change JWT expiry | `config/settings.py` (SIMPLE_JWT dict) |
| Change rate limits | `config/settings.py` (REST_FRAMEWORK dict) |
| Add a permission/role | `authentication/models.py` (ROLE_CHOICES) + `authentication/api_permissions.py` |
| Add a new API endpoint | `*/api_views.py` + `*/api_urls.py` + `*/api_serializers.py` |
| Change schema routing | `config/settings.py` (SHOW_PUBLIC_IF_NO_TENANT_FOUND) + `tenants/models.py` (Domain) |
| Deploy to production | `03_DEPLOYMENT_LOCAL_AND_RAILWAY.md` |
| Connect external system | `02_KEYCLOAK_AND_AUTHENTICATION.md` (sections 11–12) |
| Panel defense preparation | `05_PRESENTATION_DEFENSE_COMPLETE_GUIDE.md` |
